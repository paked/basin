#pragma once

#include <SDL2/SDL.h>

#include <e/sprite.hpp>
/*
struct Enemy {
  Enemy(int x, int y);

  Sprite *sprite;

  void tick(float dt);
  void render(SDL_Renderer *renderer, SDL_Point cam);

  bool move = false;
  int moveDelay = 300;
  int moveAt = 0;
};*/
